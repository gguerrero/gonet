<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NetPlexer</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <style>
      .header {
        position: sticky;
        top: 0;
        padding: 32px;
        background-color: darkcyan;
        color: white;
      }

      body {
        font-family: monospace;
        font-size: 16px;
        background-color: lightgray;
        color: slategray;
      }

      .main-content {
        margin-top: 48px;
      }

      .field-name, .field-value {
        padding: 16px;
      }
      .field-name {
        background-color: whitesmoke;
      }
      .field-value {
        background-color: darkcyan;
        color: whitesmoke;
        font-weight: 700;
      }
    </style>
</head>
<body>
  <div class="header">
    <h2>NETPlexer</h2>
  </div>
  
  <div class="main-content container">
    <div class="row">
      <div class="field-name col-12 col-md-6">
        Method
      </div>
      <div class="field-value col-12 col-md-6">
        {{ .Method }}
      </div>
    </div>

    <div class="row">
      <div class="field-name col-12 col-md-6">
        Host
      </div>
      <div class="field-value col-12 col-md-6">
        {{ .Host }}
      </div>
    </div>
  
    <div class="row">
      <div class="field-name col-12 col-md-6">
        Path
      </div>
      <div class="field-value col-12 col-md-6">
        {{ .Path }}
      </div>
    </div>

    <div class="row">
      <div class="field-name col-12 col-md-6">
        User Agent
      </div>
      <div class="field-value col-12 col-md-6">
        {{ .UserAgent }}
      </div>
    </div>

    <div class="row">
      <div class="field-name col-12 col-md-6">
        ContentType
      </div>
      <div class="field-value col-12 col-md-6">
        {{ .ContentType }}
      </div>
    </div>

    <div class="row">
      <div class="field-name col-12 col-md-6">
        ContentLength
      </div>
      <div class="field-value col-12 col-md-6">
        {{ .ContentLength }}
      </div>
    </div>

    <div class="row">
      <div class="field-name col-12 col-md-6">
        Headers
      </div>
      <div class="field-value col-12 col-md-6">
        <ul style="list-style: none; padding-left: 0;">
          {{ range $k, $v := .Headers }}
            <li>
              <span class="badge bg-light text-dark">{{ $k }}</span>
              <span>{{ $v }}</span>
            </li>
          {{ end }}
        </ul>
      </div>
    </div>

    <div class="row">
      <div class="field-name col-12 col-md-6">
        Body
      </div>
      <div class="field-value col-12 col-md-6">
        {{ .Body }}
      </div>
    </div>

    <div class="row">
      <div class="field-name col-12 col-md-6">
        Params
      </div>
      <div class="field-value col-12 col-md-6">
        <ul style="list-style: none; padding-left: 0;">
          {{ range $k, $v := .Params }}
            <li>
              <span class="badge bg-light text-dark">{{ $k }}</span>
              <span>{{ $v }}</span>
            </li>
          {{ end }}
        </ul>
      </div>
    </div>
  </div>  
</body>
</html>